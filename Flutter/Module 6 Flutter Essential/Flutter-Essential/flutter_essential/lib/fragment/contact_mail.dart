import 'package:flutter/material.dart';

class ContactMailPage extends StatelessWidget {
  const ContactMailPage ({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text("ContactMail Page"),
      ),
    );
  }
}
